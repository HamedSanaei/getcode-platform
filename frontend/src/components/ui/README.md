# UI components

Components in this folder are implementation counterparts of approved Penpot Design System components. Each component should document its Penpot component name/variant and receive visual regression coverage once the harness exists.
