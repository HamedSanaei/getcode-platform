# Feature folders

Create feature folders only after the corresponding domain/application capability exists. Suggested bounded UI features: `auth`, `catalog`, `checkout`, `orders`, `wallet`, `activations`, `admin`.

A feature may import shared UI/lib code but must not become a second business-logic implementation.
