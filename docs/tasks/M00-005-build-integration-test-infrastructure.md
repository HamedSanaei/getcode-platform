# M00-005: Build integration-test infrastructure

- Status: **DONE**
- Milestone: **M00**
- Priority: **P0**
- Depends on: M00-003

## Goal

Build integration-test infrastructure.

## Acceptance criteria

- [x] Integration tests can start isolated PostgreSQL/Redis dependencies without using developer data.
- [x] Database is migrated/seeded per test suite deterministically.
- [x] Tests cover transaction rollback and a representative persistence path.

## Required verification

- [x] integration suite from clean environment

## Engineering constraints

- Follow root `AGENTS.md` and all accepted ADRs.
- Add no production secret/credential to repository or tests.
- Keep provider/payment DTOs contained in Infrastructure when applicable.
- Update `docs/STATUS.md` and this task status when completed.

## Agent handoff

Record: files changed, decisions/assumptions, commands/tests run, migration/config/operations impact, residual risk and next unblocked task.

### Handoff (2026-08-24)

- Files changed: `Directory.Packages.props` (+ Testcontainers.PostgreSql/Redis 4.14.0, + pinned Microsoft.EntityFrameworkCore.Relational 10.0.11), `GetCode.IntegrationTests.csproj` (+ both Testcontainers modules), new `backend/tests/GetCode.IntegrationTests/Infrastructure/` (`DatabaseFixture` — per-run Postgres container shared via a collection fixture that applies EF migrations; `RedisFixture` — opt-in Redis container verified by raw RESP PING so no client package is added before a consumer exists; `GetCodeApiFactory` — WebApplicationFactory overriding only the DbContext connection string), `PersistencePathTests.cs`, `RedisIsolationTests.cs`, initial migration `backend/src/GetCode.Persistence/Migrations/*_InitialCreate.*` (generated by `dotnet ef migrations add`; formatter applied EOL/charset fixes only), `GetCode.Persistence.csproj` (+ direct Relational reference; removed an unjustified `FrameworkReference` to the ASP.NET Core shared framework).
- Bug fixed during verification: transitive EF Relational resolved to 10.0.4 in test projects while Persistence compiled against 10.0.11 → runtime `FileNotFoundException`. Pinned centrally; solution build is now 0 warnings.
- Isolation proof: with `docker compose stop postgres redis` (developer databases down) the suite still passes 6/6 — every run provisions its own containers on random ports, applies migrations into a fresh database, and discards them.
- Determinism: schema comes from committed EF migrations (`Database.MigrateAsync`), never from developer data or `EnsureCreated`.
- Verification commands: `dotnet test GetCode.IntegrationTests` twice (with dev stack up and stopped) → green; full solution suite → 22/22 across Unit/Observability/Architecture/Integration; `dotnet format --verify-no-changes` green after EOL normalization of generated migration files.
- Migration/config/operations impact: first committed EF migration exists (`InitialCreate`); CI runs these tests inside the backend job where Docker is available on GitHub runners. No application configuration changes.
- Residual risk: none known locally; GitHub runner behavior will be confirmed on the next push.
- Next unblocked task: M00-006 (durable identifiers + migration policy).
