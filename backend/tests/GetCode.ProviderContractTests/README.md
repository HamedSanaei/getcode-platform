# Provider contract tests

M04-001 turns this project into the reusable behavioral contract every virtual-number provider adapter must pass. It exists now so provider tests do not get buried in generic unit tests.
