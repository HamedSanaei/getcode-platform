# Orders domain

Domain model for the **Orders** bounded capability. Keep this folder framework-free and provider-neutral. Introduce aggregates/value objects only when a roadmap task defines their invariants.
